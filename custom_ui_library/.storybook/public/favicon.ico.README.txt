This directory is used to serve static files in Storybook.

A real favicon.ico can be placed here if desired. This placeholder README prevents the ".storybook/public" path from being missing when Storybook starts.
