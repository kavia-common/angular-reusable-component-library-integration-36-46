Optional static directory for Storybook.
Place any static assets (e.g., favicon.ico) here to be served at the root (/) of Storybook.

This file exists so that staticDirs with { skipUnresolved: true } can still be safely resolved in environments where strict checking is enabled.
